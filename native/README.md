# Native Placeholder

Ce dossier est reserve a de futurs composants `Rust` ou `C++` pour les parties
qui justifieront une optimisation apres profilage:

- parsers intensifs
- modules CPU/GPU critiques
- moteurs specialises exposes ensuite a `Python`

La V1 n'embarque pas encore de code natif.
