# UI Placeholder

Ce dossier est reserve a une future interface TypeScript pour:

- piloter les equipes d'agents
- visualiser les handoffs et les traces
- afficher les benchmarks et l'etat des runtimes locaux

La V1 reste volontairement centree sur `Python` et l'API `FastAPI`.
